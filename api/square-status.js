const {authenticate,environment,connection}=require('./square-utils');
module.exports=async(req,res)=>{const user=await authenticate(req);if(!user)return res.status(401).json({error:'Unauthorized.'});const configured=Boolean(process.env.SQUARE_APPLICATION_ID&&process.env.SQUARE_APPLICATION_SECRET&&process.env.SQUARE_REDIRECT_URL&&process.env.SQUARE_TOKEN_ENCRYPTION_KEY);try{const c=configured?await connection():null;return res.status(200).json({configured,connected:Boolean(c),environment:environment(),merchantId:c?.merchant_id||'',merchantName:c?.merchant_name||'',connectedAt:c?.connected_at||'',tokenExpiresAt:c?.token_expires_at||''});}catch(error){return res.status(500).json({error:'Unable to read the Square connection status. Confirm the Square database tables were created.'});}};
