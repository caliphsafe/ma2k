# API folder

These are Vercel serverless function starters:

- `POST /api/contact`
- `POST /api/testimonial`
- `POST /api/custom-order`

They validate requests and return a response, but they do not permanently save submissions yet. Connect them to Supabase, email, Printflow, or another service before treating them as production storage.
