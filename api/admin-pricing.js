const fs=require('fs'),path=require('path');const ALLOWED=['babaoussou@gmail.com','caliph.safe@gmail.com'];async function auth(req){const token=(req.headers.authorization||'').replace('Bearer ','');if(!token||!process.env.SUPABASE_URL||!process.env.SUPABASE_SERVICE_ROLE_KEY)return null;const r=await fetch(`${process.env.SUPABASE_URL}/auth/v1/user`,{headers:{Authorization:`Bearer ${token}`,apikey:process.env.SUPABASE_SERVICE_ROLE_KEY}});if(!r.ok)return null;const u=await r.json();return ALLOWED.includes((u.email||'').toLowerCase())?u:null}module.exports=async(req,res)=>{if(!await auth(req))return res.status(401).json({error:'Unauthorized'});if(req.method==='GET'){const file=path.join(process.cwd(),'data/products.json');return res.status(200).json(JSON.parse(fs.readFileSync(file,'utf8')))}if(req.method==='PUT'){return res.status(501).json({error:'Connect Supabase pricing table before writes are enabled.'})}res.status(405).end()}